# Core scientific stack
numpy==1.26.4
joblib==1.4.2

# Deep learning (used by agents.models / drl_agent)
torch==2.3.1

# System / process metrics (RSS 監視と再起動判定で使用)
psutil==5.9.8

# Logging / visualization (optional but referenced in utils.logger)
# If you don't need TensorBoard, you can remove this
tensorboard==2.16.2

# TensorBoard compatibility (avoid MessageToJson keyword error)
protobuf==4.25.3

# Plotting (evaluation visualization)
matplotlib==3.9.0

# Optional: build Cython extension for faster MCTS select
cython>=3.0
